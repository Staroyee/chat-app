<div class="container mt-5">
    <div class="card" style="width: 30rem;">
        <h5 class="card-header p-3">Account Details</h5>
        <div class="card-body">
            <h5 class="card-title">{{ username }}</h5>
            <p class="card-text">{{ address }}</p>
            <p class="card-text">{{ email }}</p>
            <p class="card-text">{{ phone }}</p>
            <button class="card-link" type="submit">Edit</button>
        </div>
    </div>
</div>

<div class="container mt-5">
    <div class="card" style="width: 50rem;">
        <h5 class="card-header p-3">Order History</h5>
    </div>
</div>

{{#each orders}}
<li>
    <div class="container mt-5">
        <div class="card" style="width: 50rem;">
            <div class="card-header d-flex justify-content-between align-items-center p-3">
                <h5>Order Number: {{ this.id }}</h5>
                <h5 class="card-summary">Total:</h5>
            </div>
            <ul>
                {{#each this.Products}}
                <li>
                    <div class="card-body">
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">Item: {{ this.product_id }}</li>
                            <li class="list-group-item">${{ this.price }}</li>
                        </ul>
                    </div>
                </li>
                {{/each}}
            </ul>
        </div>
    </div>
</li>
{{/each}}